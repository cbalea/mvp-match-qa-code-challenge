name: Run tests

on:
  workflow_dispatch:

jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: gem install bundler
      - run: bundle install
      - run: bundle exec rspec specs
